O objetivo desse exercicio eh  mostrar que apenas serao carregados no bundle.js

os scripts que forem referenciados que em algum momento pela aplicação.

Para executar o build da aplicação bastou executar o comando abaixo:

    webpack

Porém modulo do webpack precisou ser instalado de forma global através do comando:

    npm install -g webpack

